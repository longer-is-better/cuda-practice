#include <iostream>
#include <iomanip> 
int main(){
    // float delta = 0.1, l_a = 0, a = l_a + delta;
    // while (!(a - l_a < delta * 0.1)) {
    //     l_a = a;
    //     a += delta;
    // }
    // std::cout << std::setprecision(10) << a << std::endl;
    // std::cout << std::setprecision(10) << a + delta << std::endl;
    std::cout << std::setprecision(10) << 262144.0625f + 0.0157f << std::endl;
    return 0;
}

// #include<math.h>
// #include<limits>

    // float fmin = std::numeric_limits<float>::min();
    // float fmax = std::numeric_limits<float>::max();
    // double ans = static_cast<double>(fmax) - static_cast<double>(fmin);
    // std::cout << fmax << std::endl;
    // std::cout << 2 * fmax << std::endl;
    // std::cout << fmin + fmax << std::endl;
    // std::cout << ans << std::endl;
    // std::cout << static_cast<double>(fmax) - static_cast<double>(fmin) << std::endl;

